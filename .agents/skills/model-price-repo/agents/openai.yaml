interface:
  display_name: "Model Price Repo"
  short_description: "Repo-specific patterns and workflows for model-price-repo"
  default_prompt: "Use the model-price-repo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true